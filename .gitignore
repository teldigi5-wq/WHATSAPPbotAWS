node_modules/

# WhatsApp session & database — stored on /data (EBS), NOT in the repo
/data/
/tmp/

# PM2 logs
*.log

# OS files
.DS_Store
Thumbs.db

# Railway-specific files (no longer needed on AWS)
railway.toml
nixpacks.toml

# Secrets & backups — never commit these
*.pem
*.backup*
ecosystem_config.js
